import {describe,it,expect} from 'vitest';import {parseGoogleDriveFileId} from '../src/lib/drive-url';import {canAccessTeacher} from '../src/lib/rbac';import {normalizeAnalysis} from '../src/services/ai/schema';import {upsertRow,docReplacements} from '../src/services/google';
const result={studentName:null,overallConfidence:.8,reviewRequired:false,score:999,maxScore:999,percentage:999,questions:[{questionNumber:'1',studentAnswer:'a',teacherCorrectionDetected:false,teacherCorrection:null,expectedAnswer:'a',isCorrect:true,scoreAwarded:3,maxScore:2,confidence:.8,reviewRequired:false,reason:null}],summary:'',warnings:[]};describe('core safety rules',()=>{it('parses supported Drive links',()=>expect(parseGoogleDriveFileId('https://drive.google.com/file/d/abcDEF_12345/view')).toBe('abcDEF_12345'));it('rejects non Google hosts',()=>expect(()=>parseGoogleDriveFileId('https://evil.test/file/d/abcDEF_12345')).toThrow());it('enforces ownership',()=>{expect(canAccessTeacher({id:'t',role:'TEACHER'},{userId:'t',ldmId:'l'})).toBe(true);expect(canAccessTeacher({id:'x',role:'TEACHER'},{userId:'t',ldmId:'l'})).toBe(false)});it('recalculates scores and confidence status',()=>{const n=normalizeAnalysis(result,2,.85);expect(n.score).toBe(2);expect(n.percentage).toBe(100);expect(n.reviewRequired).toBe(true)});it('upserts sheet records without duplicates',()=>expect(upsertRow([['record_id','score'],['a',1]],{record_id:'a',score:2})).toEqual([['record_id','score'],['a',2]]));it('builds Docs replacement requests',()=>expect(docReplacements({teacher_name:'Անի'})[0].replaceAllText.replaceText).toBe('Անի'))});
